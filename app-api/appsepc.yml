version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/physical-therapy-app-api
permissions:
  - object: /home/ubuntu/physical-therapy-app-api/
    owner: ubuntu
    group: ubuntu
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
      runas: ubuntu
